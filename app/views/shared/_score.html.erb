<%# partial: display match scores for each player %>
<%= "#{user_score.score_set1}-#{opponent_score.score_set1}" %>
<%= "#{user_score.score_set2}-#{opponent_score.score_set2}" %>
<% if user_score.score_tiebreak + opponent_score.score_tiebreak > 0 %>
  <%= "#{user_score.score_tiebreak}-#{opponent_score.score_tiebreak}" %>
<% end %>
