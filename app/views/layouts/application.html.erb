<!DOCTYPE html>
<html>
  <head>
    <style>
      /* styling for the self written messages in the chatroom */
      <%= ".msg-#{current_user&.id}" %> {
      /* background-color: #007bff !important; */
      background-color: #ffc65a !important; /* yellow */
      color: #336699 !important; /* tennis-blue */
      opacity: 90%;
      padding: 5px;
      }
      <%= ".cont-#{current_user&.id}" %> {
      display: flex;
      justify-content: flex-end;
      }
    </style>

    <!-- Bootstrap: the script below enables the dropdown in the navbar
    thanks to https://stackoverflow.com/questions/71080857/why-the-navbar-dropdown-in-bootstrap-5-doesnt-work -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <title><%= meta_title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= meta_description %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= favicon_link_tag "favicon.png", rel: "icon", type: "image/png" %>
</head>

  <body>
    <div class="content">
      <%= render "shared/navbar" %>
      <%= render "shared/flashes" %>
      <%= yield %>
    </div>
    <footer class="footer-wrap">
      <%= render "shared/footer" %>
    </footer>
  </body>

</html>
